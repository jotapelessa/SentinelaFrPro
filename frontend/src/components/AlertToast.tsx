"use client";

import React from "react";

/**
 * AlertToast - Unificado no WebPipAlertModal.
 * O WebPipAlertModal assume o papel de exibir o alerta de intrusão completo
 * com streaming de vídeo MSE ao vivo, métricas de zona e atalho para eventos,
 * evitando duplicação de DOM e conflitos de coordenadas z-50.
 */
export const AlertToast: React.FC = () => {
  return null;
};
